    # from pki_bridge.core.scanner import Scanner
    # scanner = Scanner()
    # pyopenssl_cert = scanner.get_cert('google.com', 443)
    # c = Cert(pyopenssl_cert, 'pyopenssl_cert', 'json')
    # print(c)
    # print(c.cert)
    # cryptography_cert = Cert(pem, 'pem', 'cryptography_cert')
    # cert = Cert(cryptography_cert.cert, 'cryptography_cert', 'json')